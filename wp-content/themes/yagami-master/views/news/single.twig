{% extends 'layout.twig' %}

{% block content %}
	{#
    |
    | Section Article
    |------------------
    #}
	<section class="py-md">
		<div class="container">
			<h1>{{ post.title }}</h1>
			<p>{{ post.date }} par <a href="{{ post.author.link }}">{{ post.author }}</a></p>
			<hr/>
			<div>{{ post.content }}</div>
		</div>
	</section>
{% endblock %}
